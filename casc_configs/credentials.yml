credentials:
  system:
    domainCredentials:
    - credentials:
      - usernamePassword:
          id: some_jenkins_user
          password: supersecure
          scope: GLOBAL
          username: some_jenkins_user
